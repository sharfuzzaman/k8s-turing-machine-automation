apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "turing-machine-chart.fullname" . }}-test-connection"
  labels:
    {{- include "turing-machine-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "turing-machine-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
